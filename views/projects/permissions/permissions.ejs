<%- include("../../partials/header") %>

<h1 class="title"><%= `Permissions for project ${projectid}` %></h1>

<table
  id="tab"
  class="ui striped selectable celled compact table"
  style="width:100%"
>
  <thead>
    <tr>
      <th>N</th>
      <th>ID</th>
      <th>OwnerID</th>
      <th>UserID</th>
      <th>UserOwnerID</th>
      <th>TenantID</th>
      <th>TenantName</th>
      <th>RoleProject</th>
      <th>RoleFlow</th>
      <th>RoleBase</th>
      <th>RoleBench</th>
      <th>MembershipType</th>
      <th>TimeCreated</th>
      <th>TimeModified</th>
    </tr>
  </thead>
  <tbody>
    <% p = data["items"]; %>
    <% firstPageToken = data["firstPageToken"] %>
    <% nextPageToken = data["nextPageToken"] %>
    <% prevPageToken = data["prevPageToken"] %>
    <% if (firstPageToken) {%>
        <a href="/projects?pageToken=<%= firstPageToken %>" class="ui toggle compact blue button">FirstPage</a>
    <% } %>
    <% if (nextPageToken) {%>
        <a href="/projects?pageToken=<%= nextPageToken %>" class="ui toggle compact green button">NextPage</a>
    <% } %>
    <% if (prevPageToken) {%>
        <a href="/projects?pageToken=<%= prevPageToken %>" class="ui toggle compact orange button">PreviousPage</a>
    <% } %>
    <% for (let i = 0; i < p.length; i++) { %>
    <tr>
      <td><%= `${i + 1}.` %></td>
      <td><a href="/projects/<%= projectid %>/permissions/<%= p[i].id %>"><%= p[i].id %></a></td>
      <td><%= id2username(p[i].ownerId) %></td>
      <td><%= id2username(p[i].user.id) %></td>
      <td><%= id2username(p[i].user.ownerId) %></td>
      <td><%= p[i].tenantId %></td>
      <td><%= p[i].tenantName %></td>
      <td><%= p[i].roleProject %></td>
      <td><%= p[i].roleFlow %></td>
      <td><%= p[i].roleBase %></td>
      <td><%= p[i].roleBench %></td>
      <td><%= p[i].membershipType %></td>
      <td><%= p[i].timeCreated %></td>
      <td><%= p[i].timeModified %></td>
    </tr>
    <% } %>
  </tbody>
</table>

<script>
  $(document).ready(function() {
    $("#tab").DataTable({
      pageLength: 10,
      lengthMenu: [ [10, -1], [10, "All"] ],
      "dom": '<"top"flip>t<"bottom"flip>'
    });
  });
</script>

<pre>
<%- jsonSyntaxHighlight(JSON.stringify(data, null, 2)) %>
</pre>

<%- include("../../partials/footer") %>

