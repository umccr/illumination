<%- include("../../partials/header") %>

<h1 class="title"><%= `Notification Subscriptions for ProjectID ${projectid}` %></h1>

<table
  id="tab"
  class="ui striped selectable celled compact table"
  style="width:100%"
>
  <thead>
    <tr>
      <th>N</th>
      <th>ID</th>
      <th>EventCode</th>
      <th>FilterExpression</th>
      <th>NotificationChannel</th>
      <th>Enabled</th>
      <th>OwnerID</th>
      <th>TimeCreated</th>
      <th>TimeModified</th>
      <th>TenantName</th>
    </tr>
  </thead>
  <tbody>
    <% p = data["items"]; %>
    <% for (let i = 0; i < p.length; i++) { %>
    <tr>
      <td><%= `${i + 1}.` %></td>
      <td><%= p[i].id %></td>
      <td><%= p[i].eventCode %></td>
      <td><%= p[i].filterExpression %></td>
      <td><%= p[i].notificationChannel.id %></td>
      <td><%= p[i].enabled %></td>
      <td><%= id2username(p[i].ownerId) %></td>
      <td><%= format_date(p[i].timeCreated) %></td>
      <td><%= format_date(p[i].timeModified) %></td>
      <td><%= p[i].tenantName %></td>
    </tr>
    <% } %>
  </tbody>
</table>

<script>
  $(document).ready(function() {
    $("#tab").DataTable({
      pageLength: 10,
      lengthMenu: [ [10, -1], [10, "All"] ],
      "dom": '<"top"flip>t<"bottom"flip>'
    });
  });
</script>

<pre>
<%- jsonSyntaxHighlight(JSON.stringify(data, null, 2)) %>
</pre>

<%- include("../../partials/footer") %>
